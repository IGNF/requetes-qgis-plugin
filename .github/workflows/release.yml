name: Release QGIS Plugin

on:
  push:
    tags:
      - '*'

jobs:
  build-release:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install zip
        run: sudo apt-get install zip -y

      - name: Build plugin zip
        run: |
          mkdir -p IGN_requetes
          rsync -av --progress . IGN_requetes/ \
            --exclude .git \
            --exclude .github \
            --exclude IGN_requetes
          zip -r IGN_requetes.zip IGN_requetes

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: "IGN_requetes.zip"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}